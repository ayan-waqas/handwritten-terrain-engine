# Treat all C++ header files in this project as C++
*.h linguist-language=C++

# Count GLSL shaders for language statistics
shaders/*.vert linguist-language=GLSL
shaders/*.frag linguist-language=GLSL

# Vendor miniaudio & external C libraries so audio code is excluded from language stats
external/** linguist-vendored
